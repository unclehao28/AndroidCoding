# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# 本地配置：可能含服务器真实路径，不随包分发（含 setup-navigation.py 生成的备份）
server/config.json
server/config.json.bak-*

# 远程仓库缓存目录（config.json 的 cacheDir 默认指向这里）
.asw-cache/

# 临时日志
*.log
