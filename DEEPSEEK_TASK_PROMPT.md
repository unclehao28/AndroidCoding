# 安卓源码工作台：给开发 Agent 的任务提示词

你是负责实现本项目的开发工程师。请读取当前目录中的源码和交接文档，直接开发并验证。不要停留在重新设计方案或生成一份漂亮的新页面。

## 本轮设置

- 本轮阶段：**P1**。用户指定其他阶段时，改为用户指定的阶段。
- 用户环境：Windows 客户端，Linux 服务器保存超大安卓源码。
- 最终目标：在同一个图形界面中查看、搜索、跳转、编辑整套源码，不按 Framework、HAL、Native、BSP 岗位限制范围。
- 当前优先级：尽快跑通真实文件和真实工具，控制实现规模与模型上下文成本。

首次执行先读：`README.md`、`HANDOFF_STATUS.md`、`docs/ACCEPTANCE.md`，再按需读取 `prototype` 中的代码。

## 当前代码的真实状态

现有项目只有 HTML/CSS/JavaScript 交互原型，10 个示例文件和 `definitions` 符号表都写在前端。搜索是浏览器字符串匹配，编辑仅在内存中，右侧出现位置是文本匹配。变量跳转、真实服务器连接、全库索引、语言服务和真实文件保存均未实现。

`fixtures/demo-tree` 是自编演示数据，不是可编译的安卓工程。`fixtures/navigation-cpp` 与 `fixtures/navigation-java` 是作用域验收工程。`navigation-cases.json` 只是跳转预期，不是已通过的测试报告。

必须基于这些事实开发，不得把演示能力或你尚未运行的测试写成已完成。

## 产品必须保留的约束

1. Windows 不复制整套安卓源码。文件读取、索引、语言服务、保存运行在 Linux 服务器端。
2. 所有用户看到统一入口，默认全库搜索。逐步接入各语言的精准导航是实现顺序，不等于把产品缩小成只面向某个模块。
3. 阅读路径连续：搜索结果、文件预览、定义/引用侧栏、返回原阅读位置和差异查看保持在同一工作台中。
4. 变量跳转是核心功能。局部变量、参数、成员、全局变量、常量以及同名遮蔽都必须有明确验收。
5. 区分“语义解析”“符号候选”“文本匹配”“关联线索”。不能把候选列表改名成精准跳转，也不能展示虚构的置信度百分比。
6. 选择定位结果时携带源码版本和目标范围，不只传函数名。编辑、索引和导航要能识别版本不一致。
7. 先做单用户/独立工作区 MVP。保留 workspace ID 边界即可；暂不建设企业权限平台、复杂账号系统、Kubernetes、向量数据库、图数据库或 AI 聊天系统。
8. 不在模型上下文中塞入全量源码。按路径、符号和必要行段读取，避免每轮重复扫描和全文输出。

## 推荐实现方式

### 部署

优先做 Linux 上的轻量 Web 服务，Windows 通过浏览器使用。开发阶段监听 `127.0.0.1`，复用用户既有 SSH 连接做端口转发，例如：

```bash
ssh -L 8765:127.0.0.1:8765 USER@SERVER
```

上面的 USER/SERVER 是占位符。不要向用户索取或把密码、私钥写进项目。这里的 SSH 用于访问工作台，用户日常查看代码不依赖终端操作。

### 技术选型

- 后端优先 Python + FastAPI + Uvicorn；采用少量依赖，记录并锁定实际验证过的版本。
- P1 可复用当前原生前端，先把数据来源改成后端 API，保留现有布局。
- 接入编辑器和 LSP 时优先 Monaco；必要时使用 Vite + TypeScript 管理前端。不要仅为重画页面引入庞大框架。
- 超大源码的长期全文检索优先评估 Zoekt。`rg` 只作为受限的初期验证或个人变更覆盖检索，不得宣称它已经解决高并发全量索引。
- C/C++ 语义导航优先 clangd；Java 语义导航评估 JDT LS。Kotlin/Rust/AIDL 等单独维护能力矩阵，不要宣称 JDT LS 支持 Kotlin。
- 元数据先用小型 JSON 或确有必要的 SQLite。不要把所有源码全文塞进 SQLite 或为原型设计复杂迁移框架。
- 启动时不运行未经许可的全量 Android 构建，不自动升级宿主机系统环境。

## P1：把原型接到真实目录

本轮默认只做这一阶段并完成交接。

交付：

1. 配置文件提供允许的源码根目录、监听地址、端口、文件大小/行段限制等，给出示例和配置校验。
2. 一个真实后端，至少提供：健康状态、分层目录列表、读取文件或行段、全文/路径搜索。
3. 前端显式区分 demo 和 real 模式。real 模式不能悄悄退回内置演示数据。
4. 搜索默认全库；初期可调用 `rg` 验证，必须限制并发、运行时间和输出数量，并可取消。默认按字面量搜索，正则作为明确选项；参数数组调用进程，禁止拼接 shell 字符串。
5. 所有文件访问解析真实路径，限制在配置根目录内，处理路径穿越、符号链接、编码、大文件及二进制。允许确有必要的多个显式根目录，不扫描整台服务器。
6. 页面保留搜索、预览、跳转历史结构；未接入语义服务的按钮显示明确的未就绪状态，不伪造跳转。
7. 用 `fixtures/demo-tree` 验证真实文件读取。换一个目录也应能工作，不依赖固定文件名。
8. 给出 Linux 启动命令、Windows 访问方法、最低必要依赖，以及当前测试结果。

P1 暂不开放真实文件写入，不伪造索引速度指标；后续编辑由 P4 完成。

## P2：真实变量和符号导航

先生成验收数据：

```bash
python scripts/prepare-navigation-fixtures.py
```

C/C++ 先通过微型工程的 clangd 导航，再验证真实安卓编译参数。随后接入 Java 微型工程和真实 AOSP Java 工程依赖。

必须做到：

- 请求参数包含 workspace、相对路径、文档版本或 hash、行列位置、导航类型。
- 支持局部变量、参数、成员变量、全局变量/常量及方法、类；区分声明、定义和引用。
- 优先请求 LSP 的 `textDocument/definition` / `declaration` / `references`；实现文档打开、修改、关闭及版本管理。
- 正确转换编辑器位置与 LSP UTF-16 行列坐标；用中文和非 BMP 字符附近的标识符补充测试。
- 根据实际 compile_commands、头文件、宏、工具链解析 C/C++。生成数据库中的路径必须属于服务器环境。
- Java 需要 source roots、依赖 jar、系统内部 API、生成源码等实际工程信息。Soong 不是 Maven/Gradle，必须有导入适配和失败诊断，不得随便放一个 JDT LS 就声称 AOSP 全部支持。
- 缺失构建环境、类路径、生成源码时返回 unavailable/incomplete，允许明确标记的候选作为辅助。
- 多个同名结果不能默认跳第一个；优先用作用域、类型和工程身份消歧，仍无法消歧时让用户选择。
- 多语言工作进程按工作区和构建目标管理，并控制数量和内存；不要每次点击都启动一个全新语言服务器。
- `fixtures/navigation-cases.json` 的预期必须由实际 LSP 返回来验证，禁止写死 fixture 文件名和返回位置来过测试。

C++ 的局部作用域解析主要由语言服务器处理，不能指望一个共享全局符号表解决所有变量跳转。共享索引也不能替代 Java 工程配置。

## P3：全库检索索引与版本一致性

- 使用 Zoekt 等成熟组件建立服务端全文索引，避免每次搜索都遍历整套源码。
- 安卓 checkout 通常由 Repo 管理多个 Git 仓库。识别实际 manifest、仓库路径与 revision 组合，保持文件相对整套源码的路径。
- 公共基线索引可以共享。个人未提交修改和未保存文档以工作区覆盖层参与结果合并、去重；不能写入别人的公共索引。
- 对比检索快照、打开文件 hash 和语言服务器文档版本；不一致时显示 stale/更新中并重新定位，不能拿旧行号直接跳转。
- `.repo` 元数据、镜像、二进制与大多数 `out` 产物不进入常规全文搜索。语义解析所需生成头文件、AIDL 产物等单独接入，不一刀切隐藏依赖。
- 支持索引进度、覆盖范围、取消、更新失败及结果截断状态。index completed 不能代表所有语言语义能力都完成。
- 记录索引首次构建、更新、内存、磁盘、冷/热检索等实测数据，不给虚构的毫秒级承诺。

## P4：真正的编辑、保存和差异

- 编辑限定在当前用户有权限的工作区；公共只读基线保持只读。
- 保存请求携带原文件 hash/版本；服务端检测文件已被其他程序修改时返回冲突，不覆盖。
- 原子保存、保留合理文件权限、编码和换行，处理磁盘不足和超时；不得对符号链接目标盲目覆盖。
- 未保存内容与已保存文件的导航版本明确区分；保存成功后刷新覆盖索引与语言服务。
- 使用可靠的 diff 能力处理插入/删除。当前原型的逐行对齐展示不能充当最终 diff。
- 增加未保存离开提醒、失败恢复和明确的真实保存状态。
- 不自动执行提交、推送、`repo sync`、`git reset --hard` 或删除源码。

## P5：真实安卓验证与后续范围

使用用户的完整源码目录开展性能和导航覆盖测试。优先获得最小必要的环境元数据；不要要求把整套公司源码上传到聊天。

按 `docs/ACCEPTANCE.md` 记录代码版本、构建目标、仓库/文件规模、硬件、首次索引、查询 p50/p95、内存/磁盘、精确/候选/失败导航数量、修改可见延迟。

待基础能力稳定，再评估 JNI 注册表、AIDL 接口、Binder 服务等跨层关联。规则推导必须有源码证据和适用范围；反射、动态注册、运行时分派无法确认时给出线索，不编造完整调用链。

## 建议的后端接口约定

这是实现约束，不是宣称当前已有接口。允许在记录理由后调整命名。

- `GET /api/health`
- `GET /api/workspaces`
- `GET /api/tree?workspace=...&path=...`
- `GET /api/file?workspace=...&path=...&startLine=...&lineCount=...`
- `POST /api/search`：query、workspace、scope、cursor/limit、请求 ID。
- `POST /api/navigation`：文件、文档版本、位置、definition/declaration/references 类型。
- `PUT /api/file`：仅 P4，包含 expectedHash 和内容。

导航响应至少包含：

```json
{
  "status": "resolved|ambiguous|unavailable|stale",
  "kind": "semantic|symbol|text|relation",
  "workspaceId": "...",
  "sourceVersion": "...",
  "targets": [
    {"path": "...", "range": {"start": {"line": 0, "character": 0}, "end": {"line": 0, "character": 1}}, "evidence": "..."}
  ],
  "reason": "..."
}
```

`|` 在上例中表示可选枚举值，真正响应只能选择一个值。统一记录坐标采用的基准和字符编码。

## 工作纪律与交接方式

1. 先确认已有代码状态，再做最少必要变更。默认保留现有产品布局。
2. 每轮执行一个指定阶段中的完整可验收增量；不要一次性输出上万行尚未运行的代码。
3. 依赖和工具缺失时说明具体缺什么，优先尝试正常安装和最小验证；不得通过假测试或默认返回成功绕过。
4. 测试覆盖真实风险：作用域解析、跨文件版本、保存冲突、路径边界、搜索取消及结果完整性。不要为凑数量写镜像实现的测试。
5. 只读必要代码片段和命令输出；不重复输出全部源码、依赖锁文件或整份设计。
6. 阶段结束更新 `HANDOFF_STATUS.md`：完成内容、真实验证命令/结果、已知限制、下一步。尽量控制在 100 行以内。
7. 无真实安卓源码时，清楚区分“微型工程通过”和“安卓全库未测”，不要冒充部署成功。
8. 默认 P1 验收后结束本轮，给出启动命令和下一阶段入口；用户说“继续 P2”时再继续，无需重做 P1。

现在开始检查项目并执行本轮阶段。先简要列出你发现的现状和本轮交付范围，随后实际改代码、运行并更新交接记录。

## 参考资料

- https://github.com/sourcegraph/zoekt
- https://clangd.llvm.org/features
- https://clangd.llvm.org/design/remote-index
- https://github.com/eclipse-jdtls/eclipse.jdt.ls
- https://android.googlesource.com/platform/build/soong/+/HEAD/docs/compdb.md

版本变化时查官方资料。不要因为网上某篇旧文章的配置不同就擅自更换总体目标。
