# 安卓源码工作台：Python 3.8 / 3.9 服务器（如 Ubuntu 20.04 自带 3.8.10）上的运行时依赖。
#
# 为什么单独一份：requirements.txt 里锁定的 fastapi 0.141 / starlette 1.6 / uvicorn 0.53
# 都要求 Python >= 3.10，在 3.8 上装不上。这里的版本组合是 pip 针对 cp38 解析出来的，
# 已确认存在 cp38/manylinux 的 wheel（pydantic-core 等编译扩展）。
#
# 安装：python3 -m pip install --user -r requirements-py38.txt
#
# 说明：这里用的是不带 [standard] 的 uvicorn（无 httptools/watchfiles 加速项），
# 功能一致，只是少了对老解释器不友好的编译扩展。
fastapi==0.115.14
starlette==0.44.0
pydantic==2.10.6
pydantic-core==2.27.2
uvicorn==0.33.0
anyio==4.5.2
h11==0.16.0
click==8.1.8
sniffio==1.3.1
typing-extensions==4.13.2
annotated-types==0.7.0
idna==3.15
